const data = require("./data");

class Result {
    constructor(data) {
    this.name = this.data
    this.result = this.data
    }
}

static get.all() 

static get.findByName(findResult) {
    const requestResult = data.filter((object) => data.result == findResult[0])
}